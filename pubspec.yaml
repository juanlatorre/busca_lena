name: busca_lena
description: Aplicación para encontrar los mejores datos de leña.
version: 1.0.0+1

environment:
  sdk: ">=2.2.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  material_design_icons_flutter: ^3.2.4595

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  fonts:
    - family: Monsalva
      fonts:
        - asset: fonts/Mansalva-Regular.ttf
